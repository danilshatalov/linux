import java.time.LocalDate;

public class Camel extends PackAnimal {
    public Camel(int animalId, String name, LocalDate birthDate, String packBehavior) {
        super(animalId, name, "Camel", birthDate, packBehavior);
    }
}
